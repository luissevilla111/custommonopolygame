import { GreaterThZeroDirective } from './greater-th-zero.directive';

describe('GreaterThZeroDirective', () => {
  it('should create an instance', () => {
    const directive = new GreaterThZeroDirective();
    expect(directive).toBeTruthy();
  });
});
