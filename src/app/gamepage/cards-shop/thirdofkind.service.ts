import { Injectable } from '@angular/core';
import { ThirdOfKind } from '../thirdofkind.model';
/*import { ShopcardsService } from './shopcards.service';*/
import { CardsModel } from '../models/cardsmodel.model';

@Injectable({
  providedIn: 'root',
})
export class ThirdofkindService {
  /*  private allCards: CardsModel[] = this.shopCardService.getCards();
  private allThird: ThirdOfKind[] = [
    new ThirdOfKind([this.allCards[0], this.allCards[1], this.allCards[2]]),
    new ThirdOfKind([this.allCards[3], this.allCards[4], this.allCards[5]]),
  ]; */
  //constructor(public shopCardService: ShopcardsService) {}
}
